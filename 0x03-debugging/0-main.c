#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry point
 * Description: prints "is it positive or negative"
 * Return: 0
 */
int main(void)
{
	int n;

	i = 0;
	positive_or_negative(i);

	return (0);
}
